spring:
  datasource:
    url: jdbc:h2:mem:study;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration/h2
    baseline-on-migrate: false
  session:
    jdbc:
      initialize-schema: never
      table-name: hd_plt_spring_session
management:
  server:
    port: -1
app:
  bootstrap:
    admin-username: admin@example.com
    admin-password: Admin-Change-Me-123!
    admin-display-name: 测试管理员
sentry:
  dsn:
  environment: test
  traces-sample-rate: 0
  send-default-pii: false
