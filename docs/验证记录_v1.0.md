# MVP 验证记录 v1.0

验证日期：2026-07-17  
验证环境：Windows、Microsoft OpenJDK 21.0.11、Maven 3.9.11、MySQL 8.4.10、Google Chrome（Playwright 驱动）

## 结论

早期MVP验证记录保留在下文。2026-07-21已完成Java运行时向25表 `hd_plt_*`
模型的切换，并在本机MySQL 8.0.46上完成Flyway、登录、权限、配置和Study读取验证。
容器镜像与浏览器视觉回归未在本次数据库切换后重新执行。

## 自动化验证

### Java 构建与集成测试

执行：

```powershell
mvn verify
```

结果：`BUILD SUCCESS`；`PlatformIntegrationTest` 共 5 项，失败 0、错误 0。
测试覆盖认证、权限、CSRF、研究创建、后端展示字段、动态配置，以及管理员
创建用户时的权限和密码哈希隔离。

### 前端与旧原型回归

执行：

```powershell
node --test tests/backend-module-boundaries.test.js
npm.cmd --prefix frontend run check
node ".\管线总览 Coverpage.test.js"
```

结果：七模块结构与 Domain/Repository 依赖方向检查通过；独立前端工程生成
4 个 `dist` 资源并通过 JavaScript 语法检查；旧原型 22 项业务逻辑测试全部通过。

## 真实 MySQL 与浏览器端到端

应用以最终 JAR 连接本机独立 MySQL 8.4.10，浏览器完成以下操作：

1. 获取 CSRF Token 并使用自建管理员登录；
2. 进入平台壳中的“临床研发管线”子 Tab；
3. 创建研究 `HD-UI-007`，页面自动刷新并展示后端返回的状态文案；
4. 进入“平台管理”，在线修改 `platform.display-name`；
5. 创建普通用户 `e2e.user2`；
6. 正常退出登录。

关键 HTTP 结果：登录 `200`、研究创建 `201`、配置更新 `200`、账号创建 `201`、退出 `200`。浏览器没有非预期控制台错误。

随后直接查询 MySQL，得到：

```text
MYSQL_VERSION=8.4.10
FLYWAY_ROWS=1
STUDY=HD-UI-007|ON_HOLD
SETTING=platform.display-name|E2E Research Platform 2|mysql-admin
USER=e2e.user2|USER|1
```

页面实测截图：[12-mvp-live-mysql.png](./screenshots/12-mvp-live-mysql.png)。

## 完整PRD目标数据库验证（2026-07-20）

使用本机MySQL 8.0.46，在执行前确认 `study_management` 不存在，然后创建空库并执行
`docs/database/V1__hd_plt_full_schema.sql`。验证结果：

```text
DATABASE=study_management
TABLES=25
ENGINE=InnoDB
COLLATION=utf8mb4_0900_ai_ci
PROJECT_STATUS_TABLES=0
PROJECT_STATUS_COLUMNS=0
TENANT_COLUMNS=0
FOREIGN_KEYS=0
CHECK_CONSTRAINTS=0
FLYWAY_HISTORY_TABLES=0
```

该段记录的是2026-07-20的建表验证快照；后续适配结果见下一节。

## hd_plt Java运行时适配验证（2026-07-21）

执行完整Maven测试和模块边界测试：

```powershell
mvn test
node --test tests/backend-module-boundaries.test.js
```

结果：Maven `BUILD SUCCESS`，集成测试11项全部通过；模块边界测试1项通过。
随后构建可执行JAR并连接本机MySQL 8.0.46启动，验证结果：

```text
HEALTH=UP
FLYWAY_VERSION=2
ROLE_COUNT=3
PERMISSION_COUNT=7
ROLE_PERMISSION_COUNT=12
SETTING_COUNT=1
SESSION_TABLE=hd_plt_spring_session
LOGIN_ROLE=ADMIN
LOGIN_PERMISSION_COUNT=7
LOGIN_DATA_SCOPE=ALL
STUDY_COUNT=0
```

用于登录验证的临时管理员及其Session已在服务停止后删除，删除后两者计数均为0。

## 验证中修复的问题

- 登录页初始化 CSRF 与快速提交之间的竞态；
- `201` 空 JSON 响应导致前端解析异常；
- 异步表单在 `await` 后继续访问已失效的 `event.currentTarget`；
- 新版浏览器 `pattern` 正则模式下用户名连字符未转义。

## 尚未在本机执行的边界

本机没有可用 Docker Engine，因此没有在本机实际运行 `docker compose up` 或构建容器镜像；Dockerfile、Compose、Prometheus、告警和 GitHub Actions 已完成静态配置。首次云部署前必须由 CI 构建镜像并在预发布环境执行相同的健康检查与浏览器冒烟。
