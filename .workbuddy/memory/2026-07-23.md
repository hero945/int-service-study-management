# 2026-07-23 工作日志

## 月报填写功能（计划 → 并行开发 → 交付）

- 计划：`~/.workbuddy/plans/blazing-cascade-babbage.md`。用户确认 3 决策：独立路由页 `/studies/:studyId/monthly-report`；管理员可填全部功能线、普通用户限团队分配的功能线；H2 补齐随本次做。旧 `/monthly` tab 不动。
- 编排：两个 agent 并行（后端 / 前端），brief 中钉死 JSON 契约（3 端点 + DTO 字段名），主 agent 抽查契约对齐后跑 clean 门禁。编排成功，契约零偏差。
- 后端交付：mysql/h2 `V12__monthly_report_permissions.sql`（monthly.read/create/update；ADMIN/USER 全量、VIEWER 仅 read）、h2 `V13__monthly_report_schema_backfill.sql`（重建月报表全列 + 新建 entry 表）、`MonthlyReportPort`/`MonthlyReportApi`/`MonthlyReportManager`/`JdbcMonthlyReportRepository`/`MonthlyReportApiService`/`MonthlyReportController`、`MonthlyReportIntegrationTest`（5 用例：物化幂等、POST 不覆盖 sequence 递增、PATCH、未分配 403、审计真列）、`SpaController` 注册、`PlatformIntegrationTest` SPA 守卫、ApiExceptionHandler 状态码映射。
- 前端交付：types/client/mock 三方法 + demo 数据 + demo 账号加 monthly.* 权限、新增 `MonthlyReportFillView.vue`、路由（monthly.read 守卫）、StudyListView 操作列「月报」按钮。
- 验证：clean 全量 —— Monthly 5/5、Platform 9/9、前端 vitest 12/12、vue-tsc+build 绿、架构测试 10/10；Flyway 应用至 v13。总计 42 测试仅 TeamMatrix 6 个预存失败。
- 新增项目级 skill：`.workbuddy/skills/study-feature-slice/SKILL.md`（两次实战验证的功能切片执行清单 + 并行编排法）。

## 月报后端 MySQL 报错修复（DISTINCT + ORDER BY）

- 现象：真实 MySQL 报 `ORDER BY ... not in SELECT list ... incompatible with DISTINCT`，源在 `JdbcMonthlyReportRepository` 两处查询（`findAssignedFunctionLines` / `findStudyFunctionLines`）。
- 根因：两查询 `SELECT DISTINCT fl.id, fl.function_code, fl.function_name ... ORDER BY fl.sort_order`，`sort_order` 未进入 SELECT 列表，MySQL 在 DISTINCT 下禁止按非选中列排序（H2 宽松不报，属漏报）。
- 修复：两处 SELECT 均补 `fl.sort_order`（mapper 只读 id/code/name，多取一列无副作用），使该列既被选中又用于排序，对 H2 与 MySQL 均合法。
- 验证：clean 重编 + 跑 MonthlyReportIntegrationTest 5/5、PlatformIntegrationTest 9/9，BUILD SUCCESS。注意：本机只能跑 H2（MODE=MySQL），真实 MySQL 行为需另行连库验证；修复后 SQL 已符合标准，预期在真实 MySQL 同样通过。

## 管理员月报可见集修正（"admin 只看到部分进展"）

- 现象：admin 进入月报只看到部分功能线/进展。用户同时误判"两个插入没有事务"。
- 事务澄清：事务**存在**——`@Transactional` 在 `MonthlyReportManager` 的 `getMonthlyReports`/`createEntry`/`updateEntry`（`manager/MonthlyReportManager.java:40/70/93`）；`@SpringBootApplication` 自动装配 `DataSourceTransactionManager`；`BusinessException extends RuntimeException` 默认回滚。Repository 无 `@Transactional` 是架构设计（Manager 持有事务边界）。应填项 INSERT（GET 物化）与进展明细 INSERT（POST 写）分属**两次 HTTP 请求=两个独立事务**，属正常 REST 设计，非"无事务"。
- 真正 bug：`fillableLines` 管理员分支返回 `findStudyFunctionLines`（该 study 团队矩阵出现的功能线），仅空集才退回全部 ACTIVE——只要 study 有部分分配，admin 就被限成只看那几条，违背计划"管理员可填全部功能线"。
- 修复：`fillableLines` 管理员分支改为 `reports.findActiveFunctionLines()`（全部 ACTIVE 功能线），普通用户仍走 `findAssignedFunctionLines`。同步改正 `MonthlyReportIntegrationTest` 管理员断言（由固定 2 条改为断言等于库中 ACTIVE 功能线总数，动态查询）。测试库种了 17 条 ACTIVE 功能线。
- 验证：clean 重编跑 Monthly 5/5 + Platform 9/9，BUILD SUCCESS。

## 月报三项增强：一行两列 + 进展删除 + 前2月历史（含跨年）

- 需求：① 页面一行展示 2 个功能线；② 每条进展支持删除；③ 每个功能线可查看前 2 个月进展（当前月 2026-07 → 2026-06/2026-05；跨年 2026-01 → 2025-12/2025-11，用 `YearMonth.minusMonths` 自动处理）。
- 后端新增能力：
  - Domain `MonthlyReportPort`：加 `deleteEntry`、`findHistoryEntries(studyId, functionLineId, List<LocalDate>)`、`findFunctionLine(id)`、`HistoryEntry` 记录。
  - Repository：软删 `UPDATE ... SET sys_deleted=1`（审计 action_code=monthly_delete）；历史查询 JOIN report 取 report_month，按 `report_month DESC, sequence_no`；`audit` 改为 5 参（加 actionCode）。
  - Manager：加 `deleteEntry`（monthly.update 权限 + `requireObjectAccess` 对象级校验 + 软删 + 审计）、`getMonthlyReportHistory`（monthly.read + 无分配则 403；`minusMonths(1/2)` 推前两月）；`fillableLines` 上一轮已修（管理员全部 ACTIVE）。
  - Api：加 `deleteEntry` 返回整页、`getMonthlyReportHistory` → `FunctionLineHistoryResponse`（`months[]` 含 `HistoryMonthResponse`）。
  - Controller：加 `DELETE /api/v1/monthly-report-entries/{entryId}`（monthly.update）、`GET /api/v1/studies/{studyId}/monthly-reports/history`（monthly.read，参数 functionLineId+month）。
- 前端：
  - `types.ts` 加 `FunctionLineHistory`；`client.ts` 加 `deleteMonthlyEntry` / `getMonthlyReportHistory`（http 实现）；`mock.ts` 实现两者（历史按传入 month 用 `minusMonths` 跨年推导，demo 数据）。
  - `main.css`：`.monthly-lines` 改 `grid-template-columns: repeat(2, minmax(0,1fr))`，窄屏 `@media (max-width:760px)` 回退单列。
  - `MonthlyReportFillView.vue`：每条进展加删除（确认弹窗 + deleting 态 + 复用 monthly.update，返回整页刷新）；每个功能线卡加「查看历史」按钮展开历史面板（懒加载前 2 月，含跨年）。
- 测试：`MonthlyReportIntegrationTest` 增至 **9 用例**（删除：软删后条目消失、审计 monthly_delete、无权限 403；历史：2026-01→2025-12/2025-11 跨年正确、无功能线分配 403），`PlatformIntegrationTest` 9/9。
- 编译坑（已排）：① Repository 历史查询不能把 `"""` 文本块嵌 `IN ("""+ph+""")`——`"""` 提前闭合块，改普通字符串拼接；② `audit` 改 5 参后 `saveEntry` 旧 4 参调用漏改；③ Service 缺 `FunctionLineHistoryResult`/`HistoryMonthResult` import、Controller 缺 `DeleteMapping` import；④ 测试 helper `mr.report_month = DATE ?`（占位符包 DATE 函数）H2 拒，改绑 `java.sql.Date.valueOf(...)`（`DATE '字面量'` 形式 OK，勿动）。
- 验证：clean 重编 → Monthly 9/9、Platform 9/9、BUILD SUCCESS；前端 `npm run check` 12/12 + vue-tsc/build 绿。全量 `mvn test` 仅 TeamMatrixIntegrationTest 6 个预存 BadSqlGrammar 失败（V9 H2 删 program_name 漂移），与本改动无关。
- ⚠️ 仍提醒：本机仅 H2（MODE=MySQL），真实 MySQL 跨年历史、软删、管理员 17 条功能线须连库点验。

## Study 列表点击 → 右侧抽屉（里程碑 / 月度进展 / 团队 / 风险）

- 需求：点击 Study 列表某行，右侧抽屉弹出 4 个 tab：① 里程碑=当前 study 的里程碑；② 月度进展=当前 study 所有职能**当前月**的月度进展；③ 团队=当前 study 团队成员；④ 风险=所有关联当前 study 的风险。
- 后端（风险 studyId 过滤，抽屉风险 tab 依赖）：
  - `RiskRepository.RiskQuery` 加 `studyId` 字段；`JdbcRiskRepository.findPage` 在状态/等级过滤后追加 `if (query.studyId() != null) { filtered.append(" AND r.study_id = ?"); args.add(query.studyId()); }`（行 49-52）。
  - 链路打通：`RiskApi.list` 签名加 `studyId` → `RiskManager.normalized()` 透传 `query.studyId()` → `RiskController.list` 加 `@RequestParam(required=false) Long studyId`。
  - 访问模型：`findPage` 先 `baseFilter(scope,…)` 做数据范围（非 admin 走 `EXISTS team_assignment` 范围子句），`studyId` 是叠加 AND 过滤；管理员（`DataScope.ALL`）跳过范围子句，studyId 直接过滤——正是抽屉要的"该 study 关联风险"。
- 前端：
  - 新增 `frontend/src/components/StudyDetailDrawer.vue`：`<Teleport to="body">` + `.drawer-overlay`（点遮罩/ Esc 关闭）+ `<aside class="study-drawer" role="dialog">`；4 tab（里程碑组表 / 月报职能线卡片 / 团队卡网格 / 风险卡），`teamCount`/`riskCount` 角标；`watch(open)` 打开即并行 load 四块数据。
  - `StudyListView.vue` 重写：行 `<tr @click="openDrawer(study)">`（`.study-row--clickable` hover 高亮），操作列 `<td @click.stop>` 保留「里程碑/月报」整页跳转按钮；底部挂载 `<StudyDetailDrawer :open :study @close>`。
  - `types.ts`/`client.ts`/`mock.ts`：`RiskQuery` 加 `studyId?`；`listRisks` 设 `if (query.studyId) parameters.set('studyId', …)`；mock 过滤 `risk.studyId === query.studyId`。
  - `main.css` 追加抽屉全量样式（overlay/drawer/tabs/tab-badge/milestone 组表/mp-line/team 卡 `repeat(2,minmax(0,1fr))`/risk 卡；`@media(max-width:520px)` 团队回退单列）。
- 测试：在 `RiskManagementIntegrationTest` 加 `filtersRisksByStudyId`——同用户赋值到 A、B 两个 study（都进数据范围），仅在 A 建 risk；断言 `studyId=A` 返回该 risk、`studyId=B` 返回 0，**隔离证明 studyId 过滤独立于范围子句**。另加 `seedStudyWith(code,suffix)` 助手（避免复用固定 TA-RISK/PROGRAM-RISK 造成唯一键冲突）。
- 验证（关键教训见下）：定向 `MonthlyReportIntegrationTest 9/9 + PlatformIntegrationTest 9/9 + RiskManagementIntegrationTest 4/4`（新增 1 个），**共 22/22 BUILD SUCCESS**，耗时 ~28s。

## ⚠️ 本机跑后端集成测试的坑（必读，省 47 分钟）

- 第一次 `clean test -pl study-management-test -am -Dtest=RiskIntegrationTest,MonthlyReportIntegrationTest,PlatformIntegrationTest` 卡死 47 分钟无输出。根因有二：
  1. **测试类名写错**：真实类是 `RiskManagementIntegrationTest`（不是 `RiskIntegrationTest`）。`-Dtest` 匹配不到 → 配合 `failIfNoSpecifiedTests=false` 实际跑了 study-management-test 下**全部 ~12 个类**的 `@SpringBootTest` 全套，慢且某类结束后 JVM 卡在 shutdown 不退出。
  2. **Windows `npm run build` 在 service 模块 `build-vue-frontend` exec 目标失败**（exit 1），`-am` 必须构建 service 模块故整条链路被环境卡住。
- 正确跑法（已验证 28s 绿）：先手动 `cd frontend && npm.cmd run build` 生成 `dist/`（含 index.html），再跑测试时**跳过前端构建**：
  `java -cp "$MAVEN_HOME/boot/plexus-classworlds-2.9.0.jar" "-Dclassworlds.conf=$MAVEN_HOME/bin/m2.conf" "-Dmaven.home=$MAVEN_HOME" "-Dmaven.multiModuleProjectDirectory=<根>" org.codehaus.plexus.classworlds.launcher.Launcher test -pl study-management-test -am -Dtest=MonthlyReportIntegrationTest,PlatformIntegrationTest,RiskManagementIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false -DfailIfNoTests=false -Dfrontend.build.skip=true`
  - `-Dfrontend.build.skip=true` 命中 `study-management-service/pom.xml` 的 `<skip>${frontend.build.skip}</skip>`，跳过 `npm run build`，但仍把已有 `../frontend/dist` 拷进 `static`（Platform SPA 守卫需要 `index.html`）。
  - 类名务必用真实名（`RiskManagementIntegrationTest`），否则又退化成跑全量。
  - 用 OS `timeout 900` 兜底，避免再次卡 47 分钟。

## Study 列表页：新增筛选框 + 新列（TA/Program/Compound/PL-PM）

- 需求（按截图，用户确认）：筛选加 **TA / Program / 阶段 / 状态** 4 个下拉；新增列 **TA / Program / Compound(产品) / PL-PM**；PL/PM 需查 team 表、后端返回 plName/pmName 两字段、前端合并成一列。范围=前端+后端补字段。
- 后端（纯加字段，零破坏契约）：
  - 领域 `Study` 早就有 `programCode/projectCode/therapeuticAreaCode`，但 `StudyView`(manager) 与 `StudyResponse`(api) 组装时丢弃——本次补回。
  - `StudyApi.StudyResponse` 加 `therapeuticAreaCode/therapeuticAreaName/programCode/projectCode/plName/pmName`（共 +6 字段）。
  - `StudyManager`：注入 `PipelineConfigRepository`（解析 `findTherapeuticAreas()` 得 TA 中文名）+ `TeamMatrixRepository`；`list()` 收集全部 studyId，`team.findRoleMemberNames(studyIds,"PL"/"PM")` 批量取成员名（**2 次查询搞定全量，无 N+1**）。
  - `TeamMatrixRepository` 新增 `Map<Long,String> findRoleMemberNames(Set<Long> studyIds, String roleCode)`；`TeamMatrixMapper` 加 `findPlPmMembers`（JOIN team_role + user，按 role_code + study_id IN 批量取 display_name）；`MybatisPlusTeamMatrixRepository` 分组后用 "、" 拼接。**空集合守卫**避免 `IN ()` 非法 SQL。
  - PL=`PL`(项目负责人)、PM=`PM`(项目经理)，role code 见 mock.ts 角色种子。
  - 新增 `StudyIntegrationTest`：seed study + PL 分配 → GET /studies 断言 `programCode=PROGRAM-RISK`、`therapeuticAreaName=肿瘤`、`plName=孙磊`、`pmName=""` —— **1/1 绿**，证明批量查询与字段补齐有效。
- 前端：
  - `types.ts` Study 加 `therapeuticAreaCode/therapeuticAreaName/programCode/projectCode/plName/pmName`（保留旧 therapeuticArea/program/product 作兜底）。
  - `StudyListView.vue` 重写：`.page-toolbar` 加 4 个 `<select>` 筛选（选项由已加载数据去重生成）+ 客户端过滤；表头顺序 TA→Program→Compound→Study No.→适应症→当前阶段→状态→PL/PM→更新时间→操作；PL/PM 列用 `plName / pmName` 合并显示（**替换原"负责人"列**）；Compound 取 `projectCode||product`，Program 取 `programCode||program`，TA 取 `therapeuticAreaName||therapeuticArea||therapeuticAreaCode`。
  - `mock.ts`：`listStudies` 从 `teamAssignments`（PL/PM 角色→userId）解析 displayName 填入 plName/pmName（dev:mock 下有数据）。
  - 验证：前端 `vue-tsc --noEmit` 0 错、`vitest run` 12/12 全过；后端 test-compile 7 模块 BUILD SUCCESS、StudyIntegrationTest 1/1 绿。
- ⚠️ 前端 `npm run build`（vite）在本机 Windows 触发 `genie-safe-delete` favicon.svg 回收错误（**环境问题，非代码**）：`vue-tsc`/单测/后端均绿，但 `dist` 未能刷新到最新前端。用户本地需绕过该 trash 错误重建 `dist` 才能看到最新 UI（或忽略，代码已验证）。
- 待确认：原"负责人"列已被 PL/PM 列替换（按截图无独立负责人列）；若用户想同时保留"负责人"列需再加回。
